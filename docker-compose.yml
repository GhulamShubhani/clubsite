services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: clubshop
    ports:
      - "5432:5432"
    volumes:
      - clubshop_pg:/var/lib/postgresql/data

volumes:
  clubshop_pg:
